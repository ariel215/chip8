[package]
name = "chippy"
version = "0.1.0"
edition = "2021"

[dependencies]
bitvec = "1.0.1"
clap = { version = "4.5.9", features = ["derive"] }
clio = { version = "0.3.5", features = ["clap-parse"] }
raylib = "5.0.1"
wasm-bindgen = { version = "0.2.93", optional = true }
chippy-lib = { path = "../chippy-lib" }
itertools = "0.14.0"
cfg-if = "1.0.1"
egui-miniquad = {version = "0.16.0", optional = true}
egui = { version = "0.31.1", optional = true }
rfd = { version = "0.15.3", optional = true }
async-std = {version = "1.13.1", optional = true } 
miniquad = { version = "0.4", optional = true } 

[features]
egui = ["dep:wasm-bindgen","dep:egui-miniquad", "dep:egui", "dep:rfd", "dep:async-std", "dep:miniquad"]
